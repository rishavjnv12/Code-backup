#include <bits/stdc++.h>

using namespace std;
void solve(){
    int n,q;
    scanf("%d%d",&n,&q);
    vector<int> a(n+1);
    for(int i=0;i<n;i++)
        cin >> a[i];
    int curr = 0;
    unordered_set<int> start;
    unordered_map<int,int> end;
    for(int i=1;i<n;i++){
        
    }
}
int main(){
    int t;
    scanf("%d",&t);
    while(t--) solve();
    return 0;
}